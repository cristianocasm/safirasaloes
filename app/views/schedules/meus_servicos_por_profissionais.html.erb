<h1>Meus Serviços</h1>

<% @myProfs.each do |pr| %>

  <div class="col-xs-12 col-md-12 stablishment">
    <div class="col-xs-12 col-md-12">
      <h3><strong><%= "#{pr.nome.titleize} (Suas Safiras: #{total_safiras(pr.id)})" %></strong></h3>
    </div>
    
    <% pr.services.each do |srv| %>
      <div class="col-xs-12 col-md-12 service<%='-scheduled' if enough_safiras?(srv.preco) %>">
        <div class="col-xs-12 col-md-8">
          <h5>Serviço: <%= srv.nome %></h5>
          <h5>Preço: <%= number_to_currency(srv.preco) %></h5>
          <h5>Recompensa: <%= pluralize(srv.recompensa_divulgacao, 'safira') %> <span><i class="fa fa-question-circle" data-toggle="popover" data-trigger="hover" data-placement="left" data-html="true" title="Recompensa por Divulgação"
          data-content="Ganhe esta recompensa marcando um horário para este serviço e, logo após, enviando as fotos do seu novo visual para o SafiraSalões."></i></span> </h5>
        </div>
        <% insert_badge if enough_safiras?(srv.preco) %>
      </div>
    <% end %>
  </div>
  
<% end %>

<%= link_to 'Voltar', customer_root_path %>